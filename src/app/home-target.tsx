import { HomeTargetSheet } from '@/components/home-target-sheet';

/** The Home quick-action gateway picker uses the native form-sheet route. */
export default function HomeTargetScreen() {
  return <HomeTargetSheet />;
}
